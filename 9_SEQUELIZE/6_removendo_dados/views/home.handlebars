<h1>Usuarios Cadastrados</h1>

<div class="container">
    {{#each users}}
    <div class="item">
        <p>{{this.name}}</p>
        <p>{{this.occupation}}</p>
        <div class="action">
            <a href="/users/{{this.id}}">Ver detalhes</a>
            <form action="/users/delete/{{this.id